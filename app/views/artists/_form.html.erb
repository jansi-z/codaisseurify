
<%= form_for @artist, html: { multipart: true } do |f| %>

  <% @artist.errors.full_messages.each do |message| %>
    <p><%= message %></p>
  <% end %>

  <%= f.text_field :name, placeholder: "Artist name" %>

  <%= f.file_field "image_url", type: :file %>

  <%= f.submit "Save" %>

<% end %>
